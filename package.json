{
  "name": "capacitor-plugin-libdc",
  "version": "0.1.0",
  "description": "Capacitor plugin for libdivecomputer integration",
  "main": "dist/plugin.js",
  "module": "dist/esm/index.js",
  "types": "dist/esm/index.d.ts",
  "scripts": {
    "build": "npm run clean && tsc && rollup -c rollup.config.js",
    "clean": "rimraf ./dist",
    "watch": "tsc --watch",
    "prepublishOnly": "npm run build"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@capacitor/core": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/ios": "^5.0.0",
    "rimraf": "^3.0.2",
    "rollup": "^2.32.0",
    "typescript": "^4.0.3"
  },
  "files": [
    "dist/",
    "ios/",
    "android/",
    "CapacitorPluginLibdc.podspec"
  ],
  "keywords": [
    "capacitor",
    "plugin",
    "native",
    "libdivecomputer",
    "dive",
    "computer"
  ],
  "capacitor": {
    "ios": {
      "src": "ios"
    },
    "android": {
      "src": "android"
    }
  }
}